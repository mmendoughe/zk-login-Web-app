{
  "name": "zk-login-web-app",
  "version": "1.0.0",
  "description": "Zero-Knowledge proof generation tool",
  "main": "webpack.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm start --prefix ./client\" \"npm start --prefix ./server\" \"npm start --prefix ./login-provider\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
